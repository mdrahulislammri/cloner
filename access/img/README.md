# Asset placeholders

Binary assets were removed because this environment does not support binary files in diffs.

Use this folder for runtime assets (e.g. `.png`, `.jpg`, `.pdf`) in deployment environments.
